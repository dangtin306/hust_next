import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Laravel API Swagger",
  description: "Laravel API Swagger documentation placeholder.",
};

export default function LaravelSwaggerPage() {
  return null;
}
