import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Laravel API Swagger",
  description: "Tài liệu Laravel API Swagger.",
};

export default function LaravelSwaggerPage() {
  return null;
}
